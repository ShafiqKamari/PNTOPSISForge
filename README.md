# PNTOPSISForge v1.0 (Conference UI)

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```

Input:
- Optional first column = alternative names.
- Optional first row under criteria = B/C types.
- Crisp scores must be integers within the selected linguistic scale range.
